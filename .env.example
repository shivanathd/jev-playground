# BYOK playground. No server key is required or used for visitor traffic.
# Do not put TYPESAFE_API_KEY here. Visitors paste their own key in the UI.
# The key is stored in the browser (sessionStorage by default) and sent only
# as an Authorization header to /api/systemone.
